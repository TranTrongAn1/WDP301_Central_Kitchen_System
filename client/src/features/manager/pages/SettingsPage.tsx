import DashboardSettingsPage from '@/features/dashboard/pages/SettingsPage';

// Giữ file này để route `/manager/settings` tiếp tục hoạt động,
// nhưng sử dụng trang Settings dùng chung cho mọi role.
export default DashboardSettingsPage;
